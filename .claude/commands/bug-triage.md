---
description: Clasificar un bug en la taxonomía A/B/C/D/E (§8) con entrevista
---

Ejecuta el flujo canónico de `/bug-triage` definido en
`AGENTS.md` § _Slash commands disponibles_ → _Definiciones canónicas_
→ `/bug-triage` para el bug descrito en `$ARGUMENTS`.

Si AGENTS.md no contiene esa sección, **parar y reportar**.
