# Variables de entorno para `demo1-ai-dlc`.
# Copia este archivo a `.env.local` y completa los valores reales.
# NUNCA commitees `.env.local` (gitignored).

# Logging
LOG_LEVEL=debug              # info | warn | error en prod

# OpenTelemetry (placeholder hasta tener collector real)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_SERVICE_NAME=demo1-ai-dlc
