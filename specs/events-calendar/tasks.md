# Tasks — events-calendar

> **Estado**: vacío. Se llena tras la firma de `design.md` (G3).
>
> Convenciones obligatorias (§6 methodology, `stack/patterns.md`):
>
> - Las tasks se ejecutan en **orden estricto** (T1 antes que T2,
>   etc.). Las bloqueadas se resuelven antes de avanzar.
> - Cada task referencia los `R*.*` que cubre.
> - Cada commit cita su task y sus `R*.*`:
>   `feat(demo1-ai-dlc): T<n> - <desc> [R<x>.<y>] AB#<wi-id>`
>   (este repo opera sin work item de ADO; el sufijo `AB#` se omite).
> - Cada test del código generado lleva `// Derived from R<x>.<y>`.

## T1..TN

_Pendiente. No escribir tasks hasta que `design.md` complete sus DEC-N
y resuelva OQ-1._
